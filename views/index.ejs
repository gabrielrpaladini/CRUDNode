<%- include('partials/header') %>

<body>
    <%- include('partials/homenavibar') %>
    <div class="container">
        
        <hr>

        <div class="jumbotron">
            <h1 class="display-4">Bem-vindo</h1>
            <p class="lead">blog</p>
            <hr class="my-4">
            <p>desc detailed</p>
        </div>
        
        <% articles.forEach(article => { %>
            <div class="card">

                <div class="card-header">
                    <h2><%= article.title %></h2>
                </div>

                <div class="card-body">
                    <a href="/<%= article.slug %>" class="btn btn-success"> Ler artigo</a>
                </div>

            </div>
            <br>
        <% }) %>
    </div>
</body>

<%- include('partials/footer') %>
